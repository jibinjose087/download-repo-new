---

- hosts: dev
  become: yes

  tasks:

    - name: download a file from git repo
      get_url:
        url: http://18.216.19.92:8081/artifactory/jibin-repo/JBoss.cmd
        dest: /tmp/JBoss.cmd
        force: yes
