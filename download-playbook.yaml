---

- hosts: dev
  become: yes

  tasks:

    - name: download a file from git repo
      get_url:
        url: https://github.com/ansarpalakottal/testJob/blob/master/test.zip
        dest: /tmp/test.zip
        force: yes
